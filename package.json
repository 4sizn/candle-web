{
  "name": "candle-web",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "homepage": "https://4sizn.github.io/candle-web/",
  "scripts": {
    "dev": "vite --mode development",
    "dev:release": "vite --mode release",
    "build:dev": "tsc && vite build --mode development",
    "build:release": "tsc && vite build --mode release",
    "preview:dev": "vite preview --mode development",
    "preview:release": "vite preview --mode release",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@types/node": "^22.13.9",
    "gh-pages": "^6.1.1",
    "typescript": "^5.2.2",
    "vite": "^5.3.1"
  },
  "dependencies": {
    "@types/three": "^0.174.0",
    "three": "^0.174.0"
  }
}
